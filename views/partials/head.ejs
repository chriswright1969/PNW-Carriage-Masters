<!doctype html>
<html lang="en-GB">
<head>
  <%
    const siteName = "PNW Carriage Masters";
    const siteUrl = "https://www.truckhearse.co.uk";
    const defaultTitle = "Truck Hearse & Lorry Hearse Hire UK | PNW Carriage Masters";
    const defaultDescription =
      "Bespoke truck hearse and lorry hearse hire across the UK. Alternative funeral transport by PNW Carriage Masters, exclusive for the funeral profession. Contact us direct for a quote.";
    const defaultImage = `${siteUrl}/images/logo.svg`;

    let rawTitle = defaultTitle;
    if (typeof metaTitle !== "undefined" && metaTitle) {
      rawTitle = String(metaTitle).trim();
    } else if (typeof title !== "undefined" && title) {
      rawTitle = String(title).trim();
    }

    const pageTitle = /PNW Carriage Masters/i.test(rawTitle)
      ? rawTitle
      : `${rawTitle} | ${siteName}`;

    const pageDescription = String(
      (typeof metaDescription !== "undefined" && metaDescription)
        ? metaDescription
        : defaultDescription
    ).trim();

    const pageImage = String(
      (typeof metaImage !== "undefined" && metaImage)
        ? metaImage
        : defaultImage
    ).trim();

    const hasCanonical = (typeof canonicalPath !== "undefined");
    const canonicalUrl = hasCanonical
      ? `${siteUrl}${String(canonicalPath || "").trim()}`
      : "";

    const phoneValue =
      (typeof settings !== "undefined" && settings && settings.phone)
        ? String(settings.phone).trim()
        : undefined;

    const jsonLd = JSON.stringify(
      [
        {
          "@context": "https://schema.org",
          "@type": "Organization",
          "name": siteName,
          "url": siteUrl,
          "logo": defaultImage,
          "telephone": phoneValue,
          "description": defaultDescription
        },
        {
          "@context": "https://schema.org",
          "@type": "Service",
          "name": "Truck hearse and lorry hearse hire",
          "serviceType": "Alternative funeral transport",
          "provider": {
            "@type": "Organization",
            "name": siteName,
            "url": siteUrl
          },
          "areaServed": {
            "@type": "Country",
            "name": "United Kingdom"
          },
          "description": defaultDescription
        },
        {
          "@context": "https://schema.org",
          "@type": "WebSite",
          "name": siteName,
          "url": siteUrl
        }
      ],
      null,
      2
    ).replace(/</g, "\\u003c");
  %>

  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <meta name="theme-color" content="#0b0f14"/>
  <meta name="robots" content="index,follow"/>

  <title><%= pageTitle %></title>
  <meta name="description" content="<%= pageDescription %>"/>

  <% if (hasCanonical) { %>
    <link rel="canonical" href="<%= canonicalUrl %>"/>
  <% } %>

  <meta property="og:type" content="website"/>
  <meta property="og:site_name" content="<%= siteName %>"/>
  <meta property="og:title" content="<%= pageTitle %>"/>
  <meta property="og:description" content="<%= pageDescription %>"/>
  <meta property="og:image" content="<%= pageImage %>"/>
  <% if (hasCanonical) { %>
    <meta property="og:url" content="<%= canonicalUrl %>"/>
  <% } %>

  <meta name="twitter:card" content="summary_large_image"/>
  <meta name="twitter:title" content="<%= pageTitle %>"/>
  <meta name="twitter:description" content="<%= pageDescription %>"/>
  <meta name="twitter:image" content="<%= pageImage %>"/>

  <link rel="stylesheet" href="/css/site.css"/>
  <link rel="icon" href="/images/logo.svg"/>

  <script type="application/ld+json"><%- jsonLd %></script>
</head>
<body>
